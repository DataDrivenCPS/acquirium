# Materialization runtime backlog

The topology-epoch reconciler is authoritative.  Remaining verification work
is tracked by tests for backend conformance, deterministic manager interleaving,
fault recovery, stale-epoch suppression, and retention-watermark compaction.
See [`materialization_epochs.md`](materialization_epochs.md) for invariants.
